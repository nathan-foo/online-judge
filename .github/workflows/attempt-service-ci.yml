name: attempt-service

on:
  push:
    branches: [main]
    paths:
      - "services/attempt-service/**"
      - ".github/workflows/attempt-service-ci.yml"
  pull_request:
    paths:
      - "services/attempt-service/**"
      - ".github/workflows/attempt-service-ci.yml"

jobs:
  attempt-service:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: services/attempt-service

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: services/attempt-service/requirements.txt
      - name: Install dependencies
        run: python -m pip install -r requirements.txt pylint black
      - name: Run black
        run: black --check app
      - name: Run pylint
        run: pylint --rcfile ../../.pylintrc app
      - name: Run build
        run: python -m compileall app
