services:
  client:
    build: ./client
    ports:
      - "3000:3000"
  gateway:
    build: ./gateway
    ports:
      - "8080:8080"
  test-service:
    build: ./services/test-service
    ports:
      - "8001:8000"